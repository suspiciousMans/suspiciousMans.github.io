// The actual content and game canvas for this route are rendered by
// HexColonyPersistent, mounted once at the app-shell level in Layout (see
// that file for why) — this route component only exists so App.jsx has
// something to attach the route's <PageMeta> to.
export default function HexColony() {
    return null;
}
